repo="/home/dorus/git/notes"
current_folder=`pwd`

cd "$repo"

#vim "${1}"
[ "${1}" == "save" ] && git pull && git add . --all && git commit -m "update" && git push || vim "${1}"


cd "$current_folder"
